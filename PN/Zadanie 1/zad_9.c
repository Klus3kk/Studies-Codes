#include <stdio.h>

int main() {
    double p,q;
    scanf("%lf%lf", &p, &q);
    printf("a + b = %lf\n", p + q);
    printf("a - b = %lf\n", p - q);
    printf("a * b = %lf\n", p * q);
    printf("a / b = %lf", p / q);
}